<?xml version="1.0"?>
<ruleset name="Sparxstar Access Manager">
    <description>Coding standards for Sparxstar Access Manager</description>

    <!-- Show progress -->
    <arg value="ps"/>
    
    <!-- Check PHP files only -->
    <arg name="extensions" value="php"/>

    <!-- Use colors -->
    <arg name="colors"/>

    <!-- What to scan -->
    <file>.</file>
    
    <!-- Exclude vendor and tests -->
    <exclude-pattern>*/vendor/*</exclude-pattern>
    <exclude-pattern>*/tests/*</exclude-pattern>
    <exclude-pattern>*/node_modules/*</exclude-pattern>

    <!-- Rules: Check PHP version compatibility -->
    <config name="testVersion" value="7.4-"/>
    <rule ref="PHPCompatibility"/>

    <!-- Rules: WordPress Coding Standards -->
    <rule ref="WordPress-Core">
        <!-- Allow short array syntax -->
        <exclude name="Generic.Arrays.DisallowShortArraySyntax"/>
        <exclude name="Universal.Arrays.DisallowShortArraySyntax"/>
    </rule>
    
    <rule ref="WordPress-Docs"/>
    
    <!-- Additional rules -->
    <rule ref="WordPress.WP.I18n">
        <properties>
            <property name="text_domain" type="array">
                <element value="sparxstar-access-manager"/>
            </property>
        </properties>
    </rule>

    <!-- Verify that everything is prefixed -->
    <rule ref="WordPress.NamingConventions.PrefixAllGlobals">
        <properties>
            <property name="prefixes" type="array">
                <element value="sparxstar_access_manager"/>
                <element value="SPARXSTAR_ACCESS_MANAGER"/>
                <element value="StarisianTechnologies\SparxstarAccessManager"/>
            </property>
        </properties>
    </rule>
</ruleset>
